import { StyleSheet } from 'react-native'

const StyleLoginTron = StyleSheet.create({
    loginIcon: {
        width: 45,
        height: 45,
        borderRadius: 25,
        marginHorizontal: 20
    },
})

export default StyleLoginTron
